<%= render :partial => "layouts/shared/sidebar" %>

<div class="column" id="content">
  <div class="ui grid">
  <%= render :partial => "layouts/shared/notice"%>
    <div class="ui divider"></div>
    
    <div class="ui hidden section divider"></div>
    <div class="row">
      <h1 class="ui huge header">
        Institutions
      </h1>
    </div>
    <div class="ui divider"></div>

    <div class="row">

      <table class="ui single line striped selectable table">
        <thead>
          <tr>
            <th>Name</th>
            <th>Location</th>
          <!-- <th>Contact</th>-->
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @institutions.each do |institution| %>
            <tr>
              <td><%= institution.name %></td>
              <td><%= institution.location %></td>
            <!-- <td><%#= institution.contact %></td>-->
              <% if current_user.admin?%>
                <td><%= link_to 'Show', institution, class: "ui basic button" %></td>
                <td><%= link_to 'Edit', edit_institution_path(institution), class: "ui basic button" %></td>
                <td><%= link_to 'Destroy', institution, method: :delete, data: { confirm: 'Are you sure?' }, class: "ui negative button" %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>

<%= link_to 'New Institution', new_institution_path, class: "positive ui button"  %>
